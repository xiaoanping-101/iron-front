const {chromium}=require('playwright');const fs=require('fs'),path=require('path');
(async()=>{const browser=await chromium.launch({executablePath:process.env.BROWSER_PATH||(process.platform==='win32'?'C:/Program Files/Google/Chrome/Application/chrome.exe':undefined),headless:true});try{const p=await browser.newPage();await p.goto('file:///'+path.resolve('index.html').replace(/\\/g,'/'));await p.click('#start');const result=await p.evaluate(()=>{loop.stop();muted=true;stage=0;loadStage();player.x=970;player.inv=0;placeActor(player);camera=670;message=0;enemies=[];for(let i=0;i<24;i++){let e={x:700+i*32,z:30+i*31%170,w:28,h:43,hp:3,kind:'soldier',origin:i,lift:0,aimX:-1,aimZ:0};placeActor(e);enemies.push(e)}for(let i=0;i<50;i++){render();tick++}const methods=['fillRect','drawImage','save','restore','beginPath','stroke','fillText'];let calls=0;const originals={};for(const m of methods){originals[m]=mainContext[m];mainContext[m]=function(...a){calls++;return originals[m].apply(this,a)}}const times=[];for(let i=0;i<180;i++){tick++;camera=670+i*.5;const t=performance.now();render();times.push(performance.now()-t)}for(const m of methods)mainContext[m]=originals[m];times.sort((a,b)=>a-b);return {scenario:'24 visible soldiers, 180 scrolling render calls, headless Chrome',meanMs:times.reduce((a,b)=>a+b,0)/times.length,p95Ms:times[Math.floor(times.length*.95)],mainCanvasCallsPerRender:calls/180}});fs.mkdirSync('test-results',{recursive:true});fs.writeFileSync('test-results/'+(process.argv[2]||'render-benchmark')+'.json',JSON.stringify(result,null,2));console.log(result)}finally{await browser.close()}})().catch(e=>{console.error(e);process.exitCode=1});
